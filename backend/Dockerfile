
# Dockerfile intentionally removed. This project no longer uses Docker.
# Run the backend locally per the top-level README.md:
# cd backend; python -m venv .venv; .\.venv\Scripts\Activate.ps1; pip install -r requirements.txt; uvicorn app.main:app --reload

